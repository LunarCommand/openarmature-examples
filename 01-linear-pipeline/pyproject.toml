[project]
name = "01-linear-pipeline"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "openai>=2.32.0",
    "openarmature",
    "pydantic>=2.7",
]

[tool.uv.sources]
openarmature = { path = "../../openarmature-python", editable = true }
